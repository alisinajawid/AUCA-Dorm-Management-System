PORT=4000

# Supabase Dashboard -> Project -> Connect -> URI.
# Use Direct connection for migrations/imports. For this Express backend, use Direct
# connection when IPv6 works, or Shared Pooler Session mode when your network is IPv4-only.
DATABASE_URL=postgresql://postgres.sapzwiptpeyovowxwjxc:[db-password]@aws-1-eu-central-1.pooler.supabase.com:5432/postgres?sslmode=no-verify

DATABASE_SCHEMA=dorm_mgmt
DATABASE_SSL=true

# Only set this to false if your local certificate store rejects Supabase's TLS certificate.
DATABASE_SSL_REJECT_UNAUTHORIZED=true
